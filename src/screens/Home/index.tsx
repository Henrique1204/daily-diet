import React from 'react';

import { Header, MainContainer } from '@components/index';

const Home: React.FC = () => {
	return (
		<MainContainer>
			<Header />
		</MainContainer>
	);
};

export default Home;
